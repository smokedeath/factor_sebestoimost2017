import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'error-form-date',
    templateUrl: 'error.form.date.html',
    styleUrls: ['error.form.date.css']
})

export class ErrorFormDate{}